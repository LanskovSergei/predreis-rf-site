# Обязательно: адрес бэкенда EPL_Calculator (тот же, что обслуживает основной проект).
# Без него демо работает только в офлайн-режиме (расчёт в браузере, без учёта
# геокодинга/маршрута/чеков из бэкенда).
VITE_API_URL=https://gsm.example.ru
